import React from 'react';

const Video = ({ width, height })  => <img src="/stream" width={width} height={height}/>

const Calibrate = () => <Video width={680} height={420}/>

export default Calibrate 
